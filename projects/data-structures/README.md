# Data Structures

Small, runnable Java examples for learning core data structures.

## Stack

`Stack.java` implements a fixed-capacity integer stack backed by an array. Its `main` method demonstrates adding values with `push` and removing them with `pop`.

Run the `Stack` class directly from an IDE or compile and run it from the `src` directory with a JDK.
